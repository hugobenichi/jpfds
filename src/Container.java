package jpfds;

// Contract api that takes an interface and verify propertie on the interface
// can then be combined with a Generator of concrete implementation

public interface Container {
  public boolean isEmpty();
  public boolean nonEmpty();
}
